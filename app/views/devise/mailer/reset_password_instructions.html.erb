<p>ようこそ <%= @resource.username %>!</p>

<p>パスワード変更のリクエストがありました。下記のリンクを開いて新しいパスワードを設定してください。</p>

<p><%= link_to 'パスワード変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>もしこのメールに覚えがなければ無視してください。</p>
<p>上のリンクを開いて新しいパスワードを設定するまで変更はされません。</p>
